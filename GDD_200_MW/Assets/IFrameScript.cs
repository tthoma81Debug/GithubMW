using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IFrameScript : MonoBehaviour
{
    // Start is called before the first frame update
    public bool invincible = false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
